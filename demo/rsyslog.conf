module(load="imuxsock")  # Unix socket input
module(load="imklog")    # Kernel log input

# Save all auth logs to a separate file
auth.* /var/log/auth_demo.log

# Send mail logs to a remote syslog server
mail.* @@192.168.1.100:514
